export const actionMap1 = {
    ACTION_1: (state, action) => {
        return state + action.payload;
    },
    ACTION_2: (state, action) => {
        return state + action.payload;
    }
};

export const actionMap2 = {
    ACTION_1: (state, action) => {
        return state + action.payload;
    },
    ACTION_2: (state, action) => {
        return state + action.payload;
    }
};
